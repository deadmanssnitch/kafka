# frozen_string_literal: true

require "spec_helper"

RSpec.describe Kafka::FFI::Event do
  # There is no way to create an Event so there is no way to unit test it. The
  # Event class should be exercised at the integration level.
end
